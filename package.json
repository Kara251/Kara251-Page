{
  "name": "kara251-page",
  "private": true,
  "version": "0.1.0",
  "description": "Static personal homepage for www.kara251.com deployed with Cloudflare Pages.",
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_LOG_PATH=.wrangler/logs wrangler pages dev public --ip 127.0.0.1 --port 4173",
    "check": "node --check public/site.js",
    "deploy": "WRANGLER_LOG_PATH=.wrangler/logs wrangler pages deploy public --project-name kara251-page"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
